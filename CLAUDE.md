# 프로젝트: 🎵아티스트 음원 유통 & 응원법 스트리밍 플랫폼

## 1. 프로젝트 개요

특정 아티스트의 음원(일반 버전/응원법 버전)과 관련 미디어를 안전하게 입수·가공해 제공하고, AI 챗봇으로 음악 검색과 콘서트 정보를 안내하는 플랫폼.

## 2. 기획 의도

- **미디어 유통 파이프라인 전체 사이클 학습**: 입수→검증→비동기 트랜스코딩→검수→권한기반 스트리밍이라는, 실제 음원/미디어 유통사 개발팀의 핵심 업무 흐름을 축소판으로 경험.
- **보안**: 팬 등급(일반 팬/팬클럽 회원)에 따라 응원법 버전 같은 프리미엄 콘텐츠 접근을 차등 제어 → 보안솔루션 회사 지원 시 "인가/접근제어" 역량으로 연결.
- **AI 활용**: 텍스트 기반 RAG를 넘어 "이 곡 응원법 버전 있어?", "다음 콘서트 언제야?" 같은 실사용 질의에 답하는 챗봇으로 Spring AI 학습 목표 충족.

## 3. MVP 기능

#### 🎧 음악 감상

- **곡 상세 정보** — 앨범 커버, 곡명, 앨범명, 발매일, 재생 시간, 제공 중인 미디어 버전 확인
- **일반·응원법 버전 전환** — 재생 화면에서 일반 음원과 응원법 음원을 토글해 감상
- **기본 플레이어** — 재생·일시정지, 재생 위치 이동, 이전·다음 곡, 반복 재생, 재생 속도 조절
- **최근 재생 기록** — 최근 들은 곡과 마지막 재생 위치를 저장해 이어 듣기 제공
- **팬 등급별 재생 제한** — 팬클럽 전용 콘텐츠는 잠금 아이콘으로 표시하고 권한이 없는 사용자에게 팬클럽 인증 안내 제공

#### 📣 응원법 학습

- **응원법 타임라인** — 음원 재생 시간에 맞춰 해당 구간의 응원 문구를 실시간 표시
- **응원 구간 강조** — 아티스트가 노래하는 구간과 팬이 응원하는 구간을 시각적으로 구분
- **재생 속도 조절** — 0.75배·1배 등 속도를 조절해 응원법을 단계적으로 학습
- **일반·응원법 비교** — 일반 버전과 응원법 버전을 같은 곡 상세 화면에서 전환

#### 🤖 AI 안내 챗봇

- **음악 및 미디어 검색** — “응원법이 있는 최신곡 보여줘”, “이 곡 라이브 버전 있어?” 같은 자연어 질의로 카탈로그 검색
- **콘서트 정보 안내** — 공연 일정, 장소, 입장 시간, 반입 금지 물품, 세트리스트 등 안내
- **출처 기반 답변** — 공연 공지와 운영 정책을 RAG로 검색하고 답변에 출처 표시
- **권한 검증** — 챗봇이 권한을 추측하지 않고 Spring Boot 서버가 JWT와 팬 등급을 검증
- **안전한 미디어 연결** — 챗봇은 콘텐츠 ID만 추천하고 실제 재생 URL은 서버가 권한 확인 후 발급
- **SSE 응답 스트리밍** — AI 답변을 토큰 단위로 전송해 실시간 타이핑 효과 제공
- **대화 문맥 유지** — Redis에 사용자별 챗봇 세션과 최근 질문 문맥을 TTL과 함께 저장

#### ⚙️ 미디어 처리 파이프라인

- 비동기 트랜스코딩: 업로드 완료 시 Kafka 이벤트 발행 → Consumer가 FFmpeg로 MP3/AAC 변환, 미리듣기 파일 생성
- 처리 상태 관리: UPLOADED → VALIDATING → PROCESSING → PUBLISHED 상태 플로우
- 실시간 처리 알림: SSE로 트랜스코딩 진행 상황을 관리자 화면에 전달
- 참고자료
  1. **spring-video (joejoe2)** — Spring Boot, MinIO 오브젝트 스토리지, Kafka, Debezium, FFmpeg로 만든 HLS 영상 스트리밍 애플리케이션이에요. 업로드→비디오 상태 조회(READY 될 때까지 폴링)→스트리밍까지 흐름이 ChantVault의 처리 상태 관리(UPLOADED→PROCESSING→PUBLISHED)와 거의 동일한 구조라, MVP1+2 설계할 때 가장 먼저 참고하기 좋아요. 오디오 전용으로 바꿔서 응용하면 됩니다. Spotify

  **Kafka 비동기 처리 패턴 학습용**

  1. **spring-boot-kafka-guide (Youngerjesus)** — Spring Boot에서 Kafka를 잘 쓰기 위한 방법을 정리한 레포로, Auto Commit vs Manual Commit, 리밸런싱 시 중복 처리 문제, Graceful Shutdown 시 커밋 타이밍 등 실무에서 놓치기 쉬운 부분을 깊이 다뤄요. 미디어 처리 이벤트가 중복 처리되면 트랜스코딩이 두 번 도는 문제가 생기니 이 부분 꼭 참고하세요.
  2. **Spring Kafka로 Producer/Consumer 구현하기** — 동기/비동기 전송 방식의 차이와 CompletableFuture 콜백 패턴을 실습 코드로 설명하는 한글 튜토리얼. Kafka를 처음 다뤄보신다면 입문용으로 적합해요. Spotify

  **FFmpeg 연동 (한글 실전 후기)**

  1. **SpringBoot + FFmpeg 사용 (velog)** — mp4에서 오디오만 추출해 wav로 변환한 뒤 외부 API로 전달하는 작업 경험을 정리한 글이에요. 오디오 변환이라 ChantVault의 일반/응원법 버전 트랜스코딩과 방향이 정확히 일치합니다. FFmpeg를 Java 프로세스에서 어떻게 실행하고 결과를 핸들링하는지 실전 코드 위주라 유용해요.

#### 🔐 보안 스트리밍

- 팬등급별 접근 제어: 일반 팬 / 팬클럽 회원 등급에 따라 응원법 버전 재생 권한 차등
- 재생 URL 발급: 만료 시간이 있는 Presigned URL 발급, 원본 파일 경로 비공개
- 요청 제한: 동일 계정의 과도한 재생 URL 발급 Rate Limiting

#### 👥 사용자 관리

- 로그인/인증: JWT 기반 인증 (Access/Refresh Token)
- 권한 관리: 일반 팬 / 팬클럽 회원 / 관리자
- 프로필 관리: 닉네임, 팬등급 정보 조회 및 수정

#### 🎵 음원 관리

- 음원 등록: 관리자가 곡 정보(제목, 앨범, 발매일) 작성 및 관리
- 버전 관리: 한 곡당 일반 버전 / 응원법 버전 두 가지 트랙 세트 등록
- 미디어 업로드: 원본 음원 파일 + 앨범 커버 이미지 업로드 (AWS S3 또는 MinIO 연동)
- 파일 검증: 확장자/MIME 타입/매직바이트 검사, SHA-256 해시로 중복 업로드 방지

#### 📊 관리자 기능 (백오피스)

- 검수 대기열: 업로드된 음원의 승인/반려 처리
- 재처리: 실패한 트랜스코딩 작업 재시도
- 공개 상태 관리: 곡 공개/비공개 전환 및 반려 사유 기록

# 1주차 미션

## ✅ 1주차: AI 챗봇 연동 (Spring AI + RAG+ VectorDB 활용)

- **학습 목표**: Spring AI를 활용한 챗봇 기능 구현
- **핵심 내용**
    - **Spring AI** 소개 및 설정
    - **OpenAI** API 연동 (ChatGPT)
    - **프롬프트** 설계 및 응답 처리
    - 간단한 대화 흐름 설계 및 테스트
- **미션**
    - 각자 프로젝트 소개 → **MVP, 기술 특색, 시스템 다이어그램 작성**
    - **SpringAI 기반 API 설계 고안** → LLM + RAG + VectorDB(PostgreSQL) 연동, 스케줄러로 DB 임베딩 처리 → 각자의 프로젝트 핵심 도메인 DB Table 등을 VectorDB에 임베딩해야 합니다.
    - **할루시네이션 대응 기법 연구** → 컨텍스트ㅇ 기반 응답 품질 개선 전략 고안